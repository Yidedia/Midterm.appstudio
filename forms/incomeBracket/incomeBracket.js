/*
submitBt.onclick = function() {
    let userIncome = Number(userInput.value)
    let rate = ''
    if (userIncome < 30000) {
        rate = 8
    } else if (userIncome >= 30000 || userIncome < 99999) {
        rate = 15
    } else {
        rate = 25
    }
    outputLb.value = `With your income of $${userIncome}, you are in a tax bracket of ${rate}%`
}

goAgainBt.onclick = function() {
    userInput.value = ''
    outputLb.value = ''
}

*/